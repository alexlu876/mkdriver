# Data artifacts — regeneratable from .dtm + savestates
data/raw/frames/
data/processed/

# Savestates — ~24 MB each, reproducible via scripts/record_savestates.py
data/savestates/*.sav
data/savestates/tracks_to_record.txt

# Python / uv
__pycache__/
*.pyc
.venv/
*.egg-info/
.ruff_cache/
.pytest_cache/

# macOS
.DS_Store

# wandb
wandb/

# Training run outputs (CSV logs, checkpoints)
runs/

# Claude Code local settings
.claude/settings.local.json
.claude/scheduled_tasks.lock
